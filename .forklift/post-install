#!/bin/bash
set -eu -o pipefail

SELF_DIR=$(cd $(dirname ${0}) && pwd -P)
SELF=${SELF_DIR}/$(basename ${0})

echo "Installing forklift test pallet"
ln -svfT ${SELF_DIR}/forklift-test ${FLBIN_DIR}/forklift-test
